# Reverse_wordle

Static reverse Wordle for GitHub Pages.

## Pages

- `index.html`: level selector with all 300 levels rendered as clue-square cards
- `level.html`: single level page with the clue rows and answer checker

## Deploy

Push this repo to GitHub and enable GitHub Pages from the default branch root. No build step is needed.
